如果 `@Resource` 注解上没有提供任何参数，即没有指定依赖对象的名称、类型或其他属性，那么默认情况下，
`@Resource` 注解将会按照以下规则进行依赖注入：

1. 名称匹配：如果存在与被注入字段、方法或构造函数参数类型相匹配的命名依赖对象，则将该依赖对象注入。

2. 类型匹配：如果不存在名称匹配的依赖对象，但存在与被注入字段、方法或构造函数参数类型相匹配的非命名依赖对象，则将该依赖对象注入。

3. 错误处理：如果既没有名称匹配的依赖对象，也没有类型匹配的依赖对象，则可能会发生注入失败的情况，可能会抛出异常或导致依赖对象为空。

因此，如果你在 `@Resource` 注解中没有指定任何参数，会尝试按照名称匹配或类型匹配的方式来进行依赖注入。
具体的匹配规则可能因容器或框架的实现而有所不同。

如果你想要确保依赖注入的精确性，建议使用 `@Resource` 注解提供名称或类型属性，以明确指定需要注入的依赖对象。